div(layout="row")
  div(flex="50")
    ninja-grid(no-data-text="No rooms assigned yet..." 
            grid-title="Event Rooms" 
            doc-svc="vm.docSvc" 
            list="vm.docSvc.doc.venues" 
            create-method="addVenue" 
            update-method="" 
            delete-method="removeVenue"
            add-toggle="vm.sideTab.rooms"
            fields=`{name: {input: 'text', header: 'Room Name', order: 10, width: 20, type: 'string'}, 
                    notes: {input: 'text', header: 'Event Room Notes', order: 20, width: 50, type: 'string'},
                    price: {input: 'text', header: 'Room Minimum', order: 30, width: 20, type: 'number'}}`)
  div(flex="50")
    .itemCardHeader(layout="column")
        h4 Table Map(s)
    .gridItemCell
      p This could maybe be where a table map image or links to the outstide serviec is added